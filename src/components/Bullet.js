import React from "react";

const Bullet = ({skill}) => {
    return (
        <p className="bg-ccDark text-white py-1.5 px-4 rounded-full w-fit md:text-sm">{skill}</p>
    )
};

export default Bullet